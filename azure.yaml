# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: family-financial-shield
metadata:
  template: family-financial-shield@0.0.1-beta
services:
  web:
    project: ./
    dist: dist
    language: js
    host: staticwebapp
infra:
  provider: bicep
  path: infra
