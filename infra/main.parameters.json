{
  "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentParameters.json#",
  "contentVersion": "1.0.0.0",
  "parameters": {
    "staticWebAppName": {
      "value": "${AZURE_ENV_NAME}-family-financial-shield"
    },
    "location": {
      "value": "${AZURE_LOCATION}"
    },
    "sku": {
      "value": "Free"
    },
    "repositoryUrl": {
      "value": "${AZURE_REPO_URL}"
    },
    "branch": {
      "value": "main"
    },
    "environmentName": {
      "value": "${AZURE_ENV_NAME}"
    }
  }
}
